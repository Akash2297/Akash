#include<iostream>
#include<iterator>
#include<algorithm>
#include<math.h>
#include<vector>
#include<stack>
#include<string.h>
#include<map>
#include<bitset>
#include<set>
#include<deque>
#include<queue>
#include<list>
#define ln 10000
#define pi acos(-1.0)
#define pb push_back
#define mp nake_pair
using namespace std;
int main()
{
    string str;
    set<string>akash;
    while(cin>>str){
        long long int len=str.size();
            
        string temp;
        temp.clear();
    
        for(int i=0;i<len;i++){
            
            if(str[i]>='a' && str[i]<='z'){
                temp+=str[i];
            }
            else if(str[i]>='A' && str[i]<='Z'){
                temp+=tolower(str[i]);
            }
            else{
                if(temp.size()>0){
                    akash.insert(temp);
                    temp.clear();
                }
                else{
                    continue;
                }
            }
        }
        if(!temp.empty()){
            akash.insert(temp);
            temp.clear();
        }
    }
    for (auto it=akash.begin();it!=akash.end();++it){
        cout<<*it<<endl;
    }
    return 0;
}
